import React from 'react';
import styled from 'styled-components';

const Sidebar = () => {
  return <StyleWrapper>this is side bar</StyleWrapper>;
};
const StyleWrapper = styled.aside`
  background-color: brown;
`;
export default Sidebar;
